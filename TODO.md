# TODO (Ready Now Queue)

This is a convenience view of the next “ready now” tasks from `PLAN_EXEC.md`.

Do not add tasks here unless they already exist in `PLAN_EXEC.md`.

## Ready Now (work top to bottom)

1. OPS-01 Archive legacy planning/tracking artifacts (single source of truth).
2. OPS-02 Add tracking artifacts (AUTOPILOT/PLAN_EXEC/PROGRESS/VERIFY/TODO).
3. OPS-03 Gate dashboard (`pnpm verify:gates`).
4. OPS-04 One-command verification (`pnpm verify`).
5. OPS-05 ADR system (`decisions/*`).
6. OPS-06 Update `AGENTS.md` to align; remove references to archived systems.
7. A-01 Route error boundaries + not-found UX for core pages.

